// This comment occupies a line of its own
alert('Hello');
alert('World'); // This comment follows the statement

/* multiline comments 
this is a multiline comment in the js
*/

//"use strict"; 
// this is the comment after the use strict it should not appear


let boby ='mochacafe';
alert(boby)


typeof 100n;



const bob = 'unchnaging variable';
const COLOR_RED = "#f00";
let color = COLOR_RED;
alert(color);

// also 
let age =100;
alert(age); // null to assign empty and the underfined is the default for the unassigned values.

// type of operator. 
typeof undefined // "undefined"
typeof 0 // "number"
typeof 10n // "bigint"
typeof true //"boolean"

//type of operator works  on the operand.


let agevar = prompt('hi how are you?',100);
alert(`You are ${agevar} years old.`);
// unary plus converts the string to the numeric first then performs operation. 

let a = (1+2,3+4)
// in here the the first operation is discarded and for the second operation the value is assigned.
// as the value of the variable a is at first 3 then 7 , when assigned again.